
document.addEventListener("DOMContentLoaded", async () => {

    const lastPunchMsg = document.getElementById("lastPunchMsg");
    const btnSubmit = document.getElementById("btnSubmit");
    const btnShowHistory = document.getElementById("btnShowHistory");
    const divHistory = document.getElementById("divHistory");
    

    //Get Last Punch info
    lastPunchMsg.textContent = `Last clocked ${5} at ${5}`;

    //Submit Punch
    btnSubmit.addEventListener("click", () => {
        // 
        
      })
      
    //Show Punch history
    btnShowHistory.addEventListener("click", () => {
        // 
        
      })
});